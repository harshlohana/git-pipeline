pipeline{
  agents any
    stages{
      stage("build"){
        steps{
        echo "running in build state"
        }
      }
       stage("test"){
        steps{
        echo "running in test state"
        }
      }
       stage("deploy"){
        steps{
        echo "running in deploy state"
        }
      }
    }
}
